define(function(){
	return function (is_sync_ok){
        // var param = is_sync_ok ? 'syncing' : 'error';//from css
        // var sync_status_dom = document.getElementById('sync-wrapper');
        // sync_status_dom.setAttribute('data-sync-state', param);
    }
});